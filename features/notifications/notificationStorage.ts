/**
 * Stub to satisfy features/auth imports.
 * Clears stored notifications data upon sign-out.
 */
export async function clearNotificationStorage(): Promise<void> {
  // No-op placeholder since notifications module is not installed in this workspace
}
